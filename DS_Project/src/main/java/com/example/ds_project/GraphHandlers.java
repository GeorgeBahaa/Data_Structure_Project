package com.example.ds_project;

import javafx.event.Event;
import javafx.event.EventHandler;

public class GraphHandlers {
    static class MostInfluencerHandler extends GUIEditor implements EventHandler{
        @Override
        public void handle(Event event) {

        }
    }
    static class ActiveUserHandler extends GUIEditor implements EventHandler{
        @Override
        public void handle(Event event) {

        }
    }

    static class MutualFollowersHandler extends GUIEditor implements EventHandler{
        @Override
        public void handle(Event event) {

        }
    }
    static class SuggestFollowersHandler extends GUIEditor implements EventHandler{
        @Override
        public void handle(Event event) {

        }
    }
    static class SearchHandler extends GUIEditor implements EventHandler{
        @Override
        public void handle(Event event) {

        }
    }
}
